[project]
name = "dataset-initializer"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "huggingface-hub>=1.14.0,<1.15",
  "opendal>=0.47.1",
  "kubernetes>=35.0.0",
]

[tool.uv]
package = false
